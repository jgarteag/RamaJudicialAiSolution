"""Adapters layer - secondary adapters implementing domain ports.

Adapters are the concrete implementations of the ports defined in the domain.
They handle communication with external systems (MongoDB, Bedrock, DynamoDB, Secrets Manager).
"""
